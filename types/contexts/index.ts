interface AppContextType {
  state: any,
  setState: React.Dispatch<any>
}

interface AuthContextType {
  state: any,
  setState: React.Dispatch<any>
}

export type {
  AppContextType,
  AuthContextType
}
